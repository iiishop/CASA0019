// arduino_secrets.h.example - Template for arduino_secrets.h
// Copy this file to arduino_secrets.h and fill in your actual credentials

#ifndef ARDUINO_SECRETS_H
#define ARDUINO_SECRETS_H

// WiFi Networks (supports multiple networks)
const int numNetworks = 2;

const char* ssids[] = {
  "Your-WiFi-SSID-1",
  "Your-WiFi-SSID-2"
};

const char* passwords[] = {
  "your-wifi-password-1",
  "your-wifi-password-2"
};

// MQTT Broker Configuration
const char* MQTT_HOST = "mqtt.cetools.org";
const int   MQTT_PORT = 1884;
const char* MQTT_USER = "your-mqtt-username";
const char* MQTT_PASS = "your-mqtt-password";

// MQTT Base Topic - customize this to your own path
const char* MQTT_BASE = "student/CASA0019/YourName/studyspace";

#endif
